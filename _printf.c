#include "main.h"

/**
 * _printf - 
 * varg - 
 * Return
 */
int _printf(const char *format, ...);
{
	va_list, varg
	va_start (varg format);
	int fmtType[] = {
	{%d, d_printf},
	{%i, i_printf},
	{%c, c_printf},
	{%s, s_printf},
	{'/0', Null}
};
int len = _strlen
for (int i = 0; i < strlen(format); i++);
